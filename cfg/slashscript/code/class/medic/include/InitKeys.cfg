//Connect sensitivity / zoom changer for arrowing (0)
alias +SlashComKeyWeap0 +SlashComSensContact
alias -SlashComKeyWeap0 -SlashComSensContact

// and cover call rotator (9)
alias +SlashComKeyWeap9 SlashMedCallCoverCycle

// and cover call mechanism (t/util)
alias +SlashComKeyUtil0 SlashMedCallCover

// and fake call mechanism (5)
alias +SlashComKeyWeap5 SlashMedCallFake

// and medic radar (4&8)
alias +SlashComKeyWeap4 +SlashMedRadarContact
alias -SlashComKeyWeap4 -SlashMedRadarContact
alias +SlashComKeyWeap8 +SlashMedRadarContact
alias -SlashComKeyWeap8 -SlashMedRadarContact

// and enemy uber timer (1)
alias +SlashComKeyWeap1 SlashMedTimerStock

// and enemy kritz timer (2)
alias +SlashComKeyWeap2 SlashMedTimerKritz

// and enemy quickfix timer (3)
alias +SlashComKeyWeap3 SlashMedTimerQuick
