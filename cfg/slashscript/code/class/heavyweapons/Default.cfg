//Enable sensitivity / zoom changer
bind "9"				"+SlashComSensContact"

//Bind in sandvich announcer
bind "0"				"+SlashHeaSandThrow"

//Master init code for heavy to avoid weapon switcher override.
//Defaulting to "Plus" movement mode as heavy really isn't going to spend
//much time flying, relatively speaking.
alias "SlashComMasterInit"		"SlashComMovePlusInit; SlashComWeapTogPreCon2; SlashComLOModStdInit"
